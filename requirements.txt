# Audio capture
sounddevice>=0.4.6
numpy>=1.24.0
soundfile>=0.12.1

# Music recognition
shazamio>=0.6.0

# Discogs
python3-discogs-client>=2.6.0
requests>=2.31.0  # Used directly in the Discogs transport (also transitive dep of discogs-client)

# Cover art fallback
musicbrainzngs>=0.7.1

# Display
pygame>=2.5.0
Pillow>=10.0.0
# Cover-art fetch pins the connection to a vetted IP (S-7); these are imported
# directly by src/display/cover_cache.py, not just transitively via requests.
# urllib3>=2.0 is required: the IP-pinned HTTPSConnectionPool relies on the
# server_hostname kwarg being forwarded to the connection (TLS SNI).
urllib3>=2.0.0
certifi>=2023.7.22

# HTTP / async
aiohttp>=3.9.0

# Config
PyYAML>=6.0.1

# Last.fm scrobbling
pylast>=5.1.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
