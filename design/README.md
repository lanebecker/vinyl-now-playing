# Design Prototype

React/Babel prototype for the Vinyl Now Playing display. **Design tool only — not production code.**

Open `index.html` in a browser to run it (serve from this directory, or open directly via `file://`). No build step required.

See [`DESIGN.md`](../DESIGN.md) at the repo root for the full design system spec and production handoff notes.
